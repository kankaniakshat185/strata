#pragma once

namespace strata {

// Bumped on incompatible on-disk format changes (block header's
// format_version field, per STRATA_DESIGN.md).
inline constexpr int kFormatVersion = 1;

}  // namespace strata
